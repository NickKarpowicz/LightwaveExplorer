name: Build Windows

on:
  push:
    branches:
      - dev
      - master

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Jimver/cuda-toolkit@v0.2.24
        id: cuda-toolkit
      - name: Install OneAPI
        run: |
          wget -q https://registrationcenter-download.intel.com/akdlm/IRC_NAS/2bc3d20f-d250-44f3-af36-c8f0dff2b86d/intel-cpp-essentials-2025.2.1.6_offline.exe
          intel-cpp-essentials-2025.2.1.6_offline.exe -a --silent --eula accept --install-dir D:\a\LightwaveExplorer\intel\oneapi
        shell: cmd
      - name: Install dependencies with choco and vcpkg
        run: |
          choco install -y wget pkgconfiglite --no-progress
          C:\vcpkg\vcpkg.exe install dlib[core]:x64-windows miniz:x64-windows gcem:x64-windows cairo:x64-windows
      - name: Install Qt
        uses: jurplel/install-qt-action@v4
      - name: Build LightwaveExplorer
        run: |
          call "D:\a\LightwaveExplorer\intel\oneapi\setvars.bat"
          cmake -E make_directory build
          cd build
          cmake -DMAKESYCL=1 .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE="C:/vcpkg/scripts/buildsystems/vcpkg.cmake" -T "Intel(R) oneAPI DPC++ Compiler 2025"
          cmake --build . --config Release
          cmake --fresh .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE="C:/vcpkg/scripts/buildsystems/vcpkg.cmake" -DCMAKE_CUDA_ARCHITECTURES="75;86"
          cmake --build . --config Release
        shell: cmd
      - name: Clean up distribution folder
        run: |
          cd build
          Copy-Item -Path Release -Destination LightwaveExplorerWin64 -Recurse
          Remove-Item .\LightwaveExplorerWin64\LightwaveExplorerSYCL.exp -Force
          Remove-Item .\LightwaveExplorerWin64\LightwaveExplorerSYCL.lib
      - name: Upload Windows binary
        uses: actions/upload-artifact@v4
        with:
          name: LightwaveExplorerWin64
          path: build/LightwaveExplorerWin64
